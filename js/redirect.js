class redirect {
    constructor (description, urla, urlb, relative, keephost, permanent, query) {
        this.description = description;
        this.urla = urla;
        this.urlb = urlb;
        this.relative = relative;
        this.keephost = keephost;
        this.permanent = permanent;
        this.query = query;
    }

}